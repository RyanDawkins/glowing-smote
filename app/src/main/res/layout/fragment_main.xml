<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.ryanddawkins.glowing_smote.MainActivity$PlaceholderFragment">

    <TextView
        android:id="@+id/initial_heading"
        android:text="@string/hello_world"
        android:gravity="center"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:textSize="40px"/>

    <EditText
        android:id="@+id/ip_address"
        android:inputType="phone"
        android:layout_marginTop="25px"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/initial_heading"
        android:hint="@string/host_name"/>

    <EditText
        android:id="@+id/port_number"
        android:inputType="number"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="25px"
        android:layout_below="@+id/ip_address"
        android:hint="@string/port_number"/>

    <Button
        android:id="@+id/save_button"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/port_number"
        android:layout_marginTop="25px"
        android:text="Save"/>

</RelativeLayout>
